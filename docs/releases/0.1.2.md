# MACKES MIDI Matrix 0.1.2

Patch release improving scriptable route deployment. `routes apply` now accepts
either a bare JSON route array or a route document containing a `routes` array;
both forms use the daemon's bounded atomic validation path.

All `0.1.1` functionality remains included. Physical device, audio, disconnect/
reconnect, and external-peer network qualification remain operator activities.
